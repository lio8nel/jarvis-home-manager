{
    "name": "jarvis-home-manager",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "web:run": "npm run dev --workspace=web",
        "web:test": "npm run test --workspace=web",
        "api:run": "npm run dev --workspace=api",
        "api:test": "npm run test --workspace=api"
    }
}